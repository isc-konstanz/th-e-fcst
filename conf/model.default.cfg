[General]
# Specifies the model type of the neural network
model = CNN

# Training epochs and batch size
epochs = 256
batch = 64

optimizer = adam
loss = logcosh
;metrics = mse

# Specifies whether a portion of input features should be used
# to validate the training process and stop early if possible
;early_stopping = False


[Resolution 1]
minutes = 180
steps_prior = 24
steps_horizon = 72

[Resolution 2]
minutes = 60
steps_prior = 24
steps_horizon = 36

[Resolution 3]
minutes = 15
steps_prior = 48
steps_horizon = 48


[Features]
;estimate = True

target = [ "pv_power" ]

input =  [ "ghi",
           "dni",
           "dhi",
           "temp_air",
           "wind_speed",
           "wind_direction",
           "humidity_rel",
           "pressure_sea",
           "total_clouds",
           "low_clouds",
           "mid_clouds",
           "high_clouds",
           "precipitation",
           "snow_fraction",
           "solar_elevation",
           "solar_azimuth",
           "solar_zenith",
           "ghi_pv_power_doubt",
           "dni_pv_power_doubt",
           "dhi_pv_power_doubt",
           "day_of_year_sin",
           "day_of_year_cos",
           "day_of_week_sin",
           "day_of_week_cos",
           "horizon" ]

cyclic = { "day_of_year": 365,
           "day_of_week": 7 }

doubt =  { "ghi": "pv_power",
           "dni": "pv_power",
           "dhi": "pv_power" }

# Time interval in hours, for which the rolling covariance of the doubt feature will be calculated
;doubt_interval = 24

[Conv1D]
layers = 3
filters = 32

activation = relu
kernel_size = 2
kernel_initializer = he_uniform

pool_size = 2

[LSTM]
;layers = 1
units = 32

activation = relu
kernel_initializer = he_uniform

[Dense]
layers = 3
units = 128
;dropout = 0.1

activation = relu
kernel_initializer = he_uniform

leaky_alpha = 0.001
